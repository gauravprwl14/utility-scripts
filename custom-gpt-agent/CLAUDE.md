# CLAUDE.md — custom-gpt-agent

System prompt for an OpenAI Custom GPT that auto-detects conversation types (meetings, support, brainstorms, etc.) and generates organized multi-file summaries with quality flags, cross-references, and Obsidian-compatible metadata.

**Format**: Markdown prompt (no code) | **Platform**: OpenAI Custom GPT builder
