# plot.md — claude-code-credentials-switch

CLI tool to swap API credentials across JSON config files from a centralized source. Supports inline and external credential definitions with configurable property mapping and nested destination paths.

**Stack**: Node.js, dotenv | **Entry**: `node switch-credentials.js`
