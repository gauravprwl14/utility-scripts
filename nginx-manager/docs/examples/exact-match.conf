# Generated by:
#   sudo nginx-manager add landing 7000 --path /landing --exact
#
# "location = /landing" only matches the exact path.
# A request to /landing/about would NOT match this block.
# Use for single-page apps, health-check endpoints, or root-level handlers.
#
# nginx-manager: managed config
# app-name: landing
# port: 7000
# path: /landing
# auth: none
# websocket: false
# exact: true
# created-at: 2026-02-22T10:00:00.000Z
# domain: rnd.blr0.geekydev.com

location = /landing {
    proxy_pass http://localhost:7000;
    include /etc/nginx/snippets/proxy-params.conf;
}
