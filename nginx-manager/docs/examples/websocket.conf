# Generated by:
#   sudo nginx-manager add vite-app 5173 --websocket
#
# Use for: Vite, Next.js dev server, socket.io, any app with HMR or long-lived connections.
# The 7-day timeouts prevent nginx from closing idle WebSocket connections.
#
# nginx-manager: managed config
# app-name: vite-app
# port: 5173
# path: /vite-app
# auth: none
# websocket: true
# exact: false
# created-at: 2026-02-22T10:00:00.000Z
# domain: rnd.blr0.geekydev.com

location /vite-app {
    proxy_pass http://localhost:5173;
    include /etc/nginx/snippets/proxy-params.conf;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_connect_timeout 7d;
    proxy_send_timeout 7d;
    proxy_read_timeout 7d;
}
