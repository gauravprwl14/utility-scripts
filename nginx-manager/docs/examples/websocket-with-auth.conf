# Generated by:
#   sudo nginx-manager add studio 6000 --path /studio --auth token --websocket
#
# nginx-manager: managed config
# app-name: studio
# port: 6000
# path: /studio
# auth: token
# websocket: true
# exact: false
# created-at: 2026-02-22T10:00:00.000Z
# domain: rnd.blr0.geekydev.com

location /studio {
    proxy_pass http://localhost:6000;
    include /etc/nginx/snippets/proxy-params.conf;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection $connection_upgrade;
    proxy_connect_timeout 7d;
    proxy_send_timeout 7d;
    proxy_read_timeout 7d;
    include /etc/nginx/snippets/security-token-header.conf;
}
